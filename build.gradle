buildscript {
    apply from: 'project.gradle', to: buildscript
}

apply plugin: 'yamoney-library-project-plugin'

groupIdSuffix = "common"
artifactID = 'db-queue-scheduler'

dependencies {
    api project(':db-queue-scheduler-core')
    api project(':db-queue-scheduler-spring')
}

subprojects {
    apply plugin: 'yoomoney-module-project-plugin'

    moduleProject {
        publishArtifactEnabled = true
    }
}